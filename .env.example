# Salesforce Integration Environment Variables
# 
# INSTRUCTIONS:
# 1. Copy this file to `.env` in the project root
# 2. Fill in your actual Salesforce Connected App credentials
# 3. Never commit the `.env` file to version control (already covered by .gitignore)
#
# All variables below are required for Salesforce OAuth 2.0 authentication

# SALESFORCE_CLIENT_ID
# The Consumer Key from your Salesforce Connected App
# Where to find: Setup → App Manager → [Your Connected App] → View → Consumer Key
# Format: Long alphanumeric string (e.g., "3MVG9...")
SALESFORCE_CLIENT_ID=

# SALESFORCE_CLIENT_SECRET (OPCIONAL quando usando PKCE)
# The Consumer Secret from your Salesforce Connected App
# Where to find: Setup → App Manager → [Your Connected App] → View → Consumer Secret
# Note: You may need to click "Click to reveal" to see the secret
# Format: Long alphanumeric string
#
# IMPORTANTE: Quando usando PKCE (recomendado), o Client Secret NÃO é necessário.
# Deixe esta linha comentada ou vazia para permitir login em qualquer org Salesforce.
# Com PKCE, você NÃO precisa instalar o Connected App em cada org que deseja conectar.
#
# SALESFORCE_CLIENT_SECRET=

# SALESFORCE_CALLBACK_URL
# The OAuth callback URL that Salesforce will redirect to after authentication
# This must EXACTLY match the Callback URL configured in your Connected App
# Development example: http://localhost:5173/api/auth/salesforce/callback
# Production example: https://yourdomain.com/api/auth/salesforce/callback
SALESFORCE_CALLBACK_URL=http://localhost:5173/api/auth/salesforce/callback

# SALESFORCE_LOGIN_URL
# The Salesforce login endpoint URL
# Use https://login.salesforce.com for production orgs and Developer Edition orgs
# Use https://test.salesforce.com for sandbox orgs
# Default: https://login.salesforce.com
SALESFORCE_LOGIN_URL=https://login.salesforce.com

# Supabase Configuration
# Get these values from your Supabase project settings
# Project Settings → API → Project URL and service_role key
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Public Supabase Configuration (for client-side auth)
# Get these from: Project Settings → API → Project URL and anon/public key
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
